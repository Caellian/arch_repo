#!/bin/sh
# Pull packages from S3 after checkout
# $3 = 1 for branch checkout, 0 for file checkout
[ "$3" = "1" ] || exit 0
just pull 2>/dev/null || echo "Warning: S3 pull failed (aws cli configured?)"
